cmake_minimum_required(VERSION 3.10)
project(energy)
find_package(OpenCV REQUIRED)
add_executable(energy main.cpp)
target_include_directories(energy PUBLIC ${OpenCV_INCLUDE_DIRS})
target_link_libraries(energy ${OpenCV_LIBS})
